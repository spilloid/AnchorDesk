# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

coder/
*storybook.log

database/data/*

# k8s secrets (real values) — never commit
k8s/dev/secrets.env

# local backend env
backend/.env
.env

# Claude Code working memory — not for the public repo
memory/

# Legacy proxy runtime data (held an RSA key + sqlite — purged from history)
proxy/data/
